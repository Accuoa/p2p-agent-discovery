---
import Landing from '../layouts/Landing.astro';
import SiteFooter from '../components/SiteFooter.astro';
import { ContentSchema } from '../content/config';
import contentJson from '../content.example.json';

const content = ContentSchema.parse(contentJson);
---
<Landing content={content} pageTitle="Terms">
  <article class="prose dark:prose-invert">
    <h1>Terms</h1>
    <p data-testid="terms-body">
      Terms of service text is provided by <code>shared-content/terms.md</code> and
      injected by the repo scaffolder.
    </p>
  </article>
</Landing>
<SiteFooter content={content} />
