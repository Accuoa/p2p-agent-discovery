{
  "name": "p2p-agent-discovery",
  "version": "0.1.0",
  "description": "Portable agent manifest format and deterministic capability-matching algorithm. Discover agents by claim without a central registry.",
  "private": true,
  "type": "module",
  "bin": {
    "p2pad": "src/cli.mjs"
  },
  "scripts": {
    "start": "node src/cli.mjs",
    "test": "vitest run",
    "benchmark": "node benchmark/run.mjs",
    "lint": "prettier --check ."
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "prettier": "^3.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}
